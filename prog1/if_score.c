#include <stdio.h>

int main(void){
	int n, score, count;
	count = 0;
	scanf("%d", &n);
	for(int i = 0; i < n; i++){
		scanf("%d", &score);
		if(){
			
		}		
	}
	printf("All pass is %d", count);
	return 0;
}
